/**
 * Layout das rotas do Painel SISTEMA.
 * Isolado das rotas de tenant.
 */
export default function SystemLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return <>{children}</>
}
